apply plugin: 'maven-publish'

publishing {
	publications {
		maven(MavenPublication) {
			groupId 'com.paypal'
			artifactId 'mpl'
			version '1.5.6.46'
			artifact 'PayPal_MPL-1.5.6.46.jar'
		}
	}
}

